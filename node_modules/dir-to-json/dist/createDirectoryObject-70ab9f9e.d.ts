interface Options {
    sortType?: boolean;
}
interface FileInfo {
    parent: string;
    path: string;
    name: string;
    type: string;
    children?: FileInfo[];
}

export { FileInfo as F, Options as O };
